---
import Pergament from './Pergament.astro'

export interface Props {
	contentArray: string[]
	lang: string
}

const { contentArray, lang } = Astro.props as Props
---

{
	contentArray.length > 0 && (
		<Pergament classNames={`px-5 py-1 ${lang === 'ar' ? 'text-right' : ''}`} lang={lang}>
			{contentArray.map((line) => {
				if (line !== '') {
					return <p class='text-black' set:html={line} />
				}
			})}
		</Pergament>
	)
}
